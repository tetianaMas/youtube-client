import { DateCheckDirective } from './date-check.directive';

describe('DateCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new DateCheckDirective();
    expect(directive).toBeTruthy();
  });
});
